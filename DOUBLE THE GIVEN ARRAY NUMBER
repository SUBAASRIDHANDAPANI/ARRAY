
# include<stdio.h>
void print(int arr[],int x){
    for(int i=0;i<x;i++){
        printf("%d",2*arr[i]);
    }
}

int main(){
    int arr[]={2,3,4,5};
    int size=sizeof(arr)/sizeof(arr[0]);
    print(arr,size);
}
